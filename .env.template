# Port for the Express server (server/index.js). Defaults to 3001.
PORT=3001

# Optional: HubSpot private app token for direct api.hubapi.com access.
# If unset, the server falls back to a HubSpot connection found via the
# Sprites API gateway, or an in-memory demo store if neither is available.
HUBSPOT_TOKEN=
